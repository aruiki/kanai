# KanaAI 開発エージェントの作業規約

更新: 2026-09-25。ユーザーの依頼により、AI向け開発指示をこの文書へ統一する。

## 指示と事実の扱い

- この規約はリポジトリ内の作業規約であり、実行環境の上位指示を上書きしない。
- 最新の明示的なユーザー指示を優先する。担当リードは実装方針、作業順序、AI向け文書を更新できる。
- `GOAL.md` は製品完成条件、`docs/PRODUCT_RELEASE_CONTRACT.md` は配布段階ごとの条件、`STATE.md` 冒頭は最新の引き継ぎ情報とする。
- 実際のソース、git差分、対象バイナリの実行結果を根拠にする。過去の進捗率や文書上のPASSだけを根拠にしない。
- `VERIFICATION.md` は記載された時点・コミットに対する独立検証記録である。後続の開発者が過去のFAILをPASSへ書き換えない。
- `INITIAL_PROMPT.md`、`LOOP_PROMPT.md`、`VERIFY_PROMPT.md` は開始用の短い手順とし、この規約と異なる独自の優先順位を設けない。

## 作業開始と引き継ぎ

1. この文書、GOAL、STATE冒頭、存在する検証記録、git status/diff/log、関連コードを読む。
2. 最新記録と実物に差があれば調査する。ユーザーが「開発が進んだ」と伝えたら、以前の前提で編集を続けず再読する。
3. 既存の未コミット変更を保持する。同時編集が判明したら担当範囲と検証対象を固定し、破棄・上書きしない。
4. ユーザーの現在の目的に最も近い実装上の障害を修正し、対象に合ったbuild/test/実行確認まで進める。
5. 失敗ログを読んで仮説を更新する。根拠を変えない同一修正・同一試行を繰り返さない。
6. STATE冒頭に完了内容、未解決事項、試した方法、コマンドと結果、次の具体的作業を記録する。過去ログは履歴と明示する。

## 製品と設計

Mozcを基盤とする、完全ローカルAIを備えた実用的なWindows日本語IMEを作る。
Mozcの辞書・変換・形態素処理・候補生成・履歴・OS統合を活用し、理由なく再実装しない。
Rustは新規のランキング、文脈管理、キャッシュ、個人適応、推論連携に適用する。
既存C++の移植には測定結果または具体的な保守上の理由が必要。

Fast PathはMozc変換と軽量処理から候補を返す。大型モデル、ディスクI/O、通信をキー入力の同期処理に入れない。
Slow Pathは確定文章・許可された短い文脈を非同期処理する。
AIの未起動・タイムアウト・クラッシュでもMozc入力を維持する。
世代の古い結果、候補IDの改変、未確定入力からの誤学習を拒否する。
パスワード等の保護フィールドでは文脈取得・AI・学習を停止する。

## 公開と検証

- 公開、push、リリース作成をユーザーが依頼済みなら、その範囲で準備・検証・公開まで進める。毎回同じ許可を求めない。
- ワンクリック導入はSetup.exe起動後に手動配置やコマンド入力を要しないこと。OSのUAC、再起動要求は実際の挙動として説明する。
- ビルド候補、検証済みベータ、製品完成を区別する。具体的な配布条件はリリース契約に従う。
- 機能未実装・未検証・未署名を隠さない。テストを緩めたり結果を書き換えて公開可能にしない。
- 本物のインストーラーで導入・日本語入力・アンインストールを検証する。単体テストやDLLロードだけをその代わりにしない。
- 変更に関係するテストを実施し、通った検証を理由なく繰り返さない。性能向上の主張には前後比較が必要。
- リリース検証の対象はソースコミットと成果物ハッシュで固定する。
- 最終完成判定は独立verifierがGOAL全条件を確認して行う。build担当は `.goal-complete` を作らない。

## OpenCodeでの並列開発

統括は docs/PARALLEL_DEVELOPMENT.md の作業票で独立した仕事を子へ委任する。
子の編集範囲は排他的に割り当て、共有STATE・git統合・公開は統括が担当する。
新しいセッションは OPENCODE_START.md と docs/OPENCODE_HANDOFF.md から開始する。
同じBazel/stage/packageと実機登録を並列実行しない。共有資源のロックを使用する。

## 実行の区切りと記録

計画や文書だけで実装依頼を済ませない。一方、永久実行や根拠のない進捗率を要求しない。
実行不能な権限・実機操作・必要なユーザー判断が残った場合、具体的な成果物と未検証範囲を保存して伝える。
作業の中断・引き継ぎを製品完成と混同しない。古い無限ループ指示や自動再開設定を勝手に復活させない。
